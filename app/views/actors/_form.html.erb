<%= form_with(model: actor, local: true) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :nombrereal %>
    <%= form.text_field :nombrereal %>
  </div>

  <div class="field">
    <%= form.label :fechanac %>
    <%= form.date_select :fechanac %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
